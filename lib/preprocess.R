########################################################
## preprocessing                                      ##
########################################################

#---case: making names of samples for family, genus, species---
#TagsCaseFam <- sort(rownnames(family_case))
#TagsCaseGen <- sort(rownnames(genus_case))
#TagsCaseSpe <- sort(rownnames(species_case))
#TagsCaseOtu <- sort(rownnames(otu_case))

#TagsCtrlFam <- sort(rownnames(family_ctrl))
#TagsCtrlGen <- sort(rownnames(genus_ctrl))
#TagsCtrlSpe <- sort(rownnames(species_ctrl))
#TagsCtrlOtu <- sort(rownnames(otu_ctrl))

#rownames(sp)[grep("^V", rownames(sp))] - just sampls which start from V
#rownames(sp)[-grep("^xx.|W$", rownames(sp))] # all samples without xx-* and *-W 
#rownames(sp)[grep("^Y8|^Y9", rownames(sp))] #just Y8 and Y9

